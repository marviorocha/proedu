
<div class="col-lg-12">
  <div class="card">
    <div class="card-close">
      <%= link_to fa_icon('chevron-left', text:'Voltar'), contents_path, class: 'float-right btn btn-sm btn-outline-secondary' %>
    </div>
    <div class="card-header d-flex align-items-center">
      <h3 class="h4"> <%= content_for(:title) %></h3>
    </div>
    <div class="card-body">

      <%= form_for @content do |f| %>
      <%#= render "shared/errors", object: @content %>
      <div class="form-group">
        <label class="form-control-label"><%= f.label :curse %></label>
        <%= collection_select(:content, :curse_id, Curse.all, :id, :title) %>
      </div>
      <div class="form-group">
        <label class="form-control-label"><%= f.label :title %></label>
        <%= f.text_field :title, placeholder: 'Digite sua lição', class: 'form-control' %>
      </div>

      <div class="form-group">
        <label class="form-control-label"><%= f.label :body %></label>
        <%= f.text_area :body, id: "editor", class: 'form-control' %>
      </div>
      <div class="form-group">
        <label class="form-control-label"><%= f.label :doc %></label>
        <%= f.text_field :doc, class: 'form-control' %>
      </div>
      <div class="form-group">
        <%= f.submit content_for(:title), class: 'btn btn-primary'%>

      </div>
      <% end %>
    </div>
  </div>
</div>
